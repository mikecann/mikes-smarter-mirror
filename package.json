{
  "name": "mikes-smarter-mirror",
  "version": "3.0.0",
  "description": "Mikes mirror but smarter",
  "scripts": {
    "install": "cd web && bun install && cd .. && neu update",
    "start": "bunx @neutralinojs/neu run"
  }
}
